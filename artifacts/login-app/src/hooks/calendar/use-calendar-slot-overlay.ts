/** Phase 5.x — availability slot overlay hook (not implemented in Sprint 5.1). */
export function useCalendarSlotOverlay() {
  return {
    enabled: false,
    slots: [],
    isLoading: false,
  };
}
