/** Default page size for CRM infinite-scroll lists. */
export const CRM_LIST_PAGE_SIZE = 50;

/** Safety cap for dashboard/aggregate hooks that still fetch list rows. */
export const CRM_LIST_MAX_ROWS = 500;

/** Audit log server page size (matches UI PAGE_SIZE multiples). */
export const AUDIT_LOG_PAGE_SIZE = 50;

/** Conversation inbox page size. */
export const CONVERSATION_LIST_PAGE_SIZE = 50;

/** Managed users list page size. */
export const USERS_LIST_PAGE_SIZE = 50;

/** Booking list safety cap per source table. */
export const BOOKING_LIST_MAX_ROWS = 500;
